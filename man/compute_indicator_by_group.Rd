% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00_compute.R
\name{compute_indicator_by_group}
\alias{compute_indicator_by_group}
\title{Calculer un indicateur desagrege par groupe}
\usage{
compute_indicator_by_group(
  dataset,
  group_by,
  formula,
  label,
  unit,
  na_rm_groupes = TRUE
)
}
\arguments{
\item{dataset}{Un \code{mecore::me_dataset} source.}

\item{group_by}{Nom de la colonne de regroupement (ex. "region").}

\item{formula}{Formule R a evaluer au sein de chaque groupe.}

\item{label}{Libelle de base ; chaque indicateur genere aura pour
libelle \code{"<label> (<group_by> = <modalite>)"}.}

\item{unit}{Unite de mesure.}

\item{na_rm_groupes}{Si \code{TRUE} (par defaut), ignore les lignes ou
\code{group_by} est manquant plutot que de creer un groupe "NA".}
}
\value{
Liste de \code{mecore::me_indicator}, un par modalite du groupe.
}
\description{
Calcule un indicateur separement pour chaque modalite d'une colonne
de regroupement (ex. par region, par sexe), plutot qu'une seule
valeur globale. Retourne UN indicateur par modalite.
}
\details{
Important : chaque indicateur genere reference le dataset ORIGINAL
(pas un sous-dataset derive) — sinon \code{mecheck::run_checks()} signale
a juste titre une reference vers un dataset absent de
\code{project@datasets} (bug trouve en testant sur donnees reelles). Le
filtrage par modalite reste un detail de calcul interne.
}
