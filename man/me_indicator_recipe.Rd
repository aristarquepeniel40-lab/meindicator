% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_recipe.R
\name{me_indicator_recipe}
\alias{me_indicator_recipe}
\title{Recette de calcul d'indicateur}
\usage{
me_indicator_recipe(
  dataset_name = character(0),
  label = character(0),
  formula = (function (.data) 
 {
    
    stop(sprintf("S3 class <\%s> doesn't have a constructor", class[[1]]), call. =
    FALSE)
 })(),
  unit = character(0),
  group_by = character(0)
)
}
\arguments{
\item{dataset_name}{Nom du \code{mecore::me_dataset} cible (doit exister
dans \code{project@datasets}).}

\item{label}{Libelle lisible de l'indicateur.}

\item{formula}{Formule R a evaluer (ex. \code{~ mean(age)}).}

\item{unit}{Unite de mesure.}

\item{group_by}{Colonne de regroupement optionnelle (ex. "region").
Si fournie, produit UN indicateur par modalite plutot qu'une seule
valeur globale (voir \code{compute_indicator_by_group()}).}
}
\description{
Decrit COMMENT calculer un indicateur (quel dataset, quelle formule,
quel libelle/unite) sans encore le calculer. Permet de definir a
l'avance tous les indicateurs d'un projet, puis de les calculer en
une seule passe avec \code{compute_project_indicators()}.
}
